{
  "name": "colyseus-node-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.3",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "format": "prettier --write \"{apps,packages}/*/src/**/*\"",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.6.1",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
