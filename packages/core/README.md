# @repo/core

Core runtime helpers shared by apps and packages.

This package owns configuration readers, logging, metrics noops, validators,
health responses, shutdown handlers, and lightweight HTTP helpers.
