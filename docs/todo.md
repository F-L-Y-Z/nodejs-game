# TODO

## P0 可靠好友房

- [x] 简单大厅：创建房间、房间号加入
- [x] 多房间隔离：按 `roomId` 创建、加入、获取快照、提交动作
- [x] 完整的基础房间生命周期
  - [x] `waiting`：创建/加入后等待玩家准备
  - [x] `playing`：所有真人准备后开局
  - [x] `settling`：牌局结束后等待再次准备
  - [x] `closed`：房间关闭后不再可加入/操作
- [x] 销毁判断逻辑
  - [x] 玩家超时释放座位，由 AI 接管
  - [x] 空房间关闭并从内存移除
- [x] 玩家主动退出房间
  - [x] 开始前可退出
  - [x] 结束后可退出
  - [x] 退出后释放座位，由 AI 接管
  - [x] 最后一名真人退出后销毁房间
- [x] 开始前与结束后 4 个座位都 `准备` 后才能开始
  - [x] 真人玩家需要手动准备
  - [x] AI 座位默认已准备
  - [x] 客户端展示每个座位的准备状态
- [x] 重复登录/同账号多端处理
  - [x] 同一账号再次进入原房间时视为重连/恢复座位，不新增座位
  - [x] 同一账号在另一设备进入同一房间时，最新实例接管，旧实例后续请求标记失效
  - [x] 同一账号尝试加入其他房间时返回明确错误
  - [x] 开发调试提供清除登录缓存/强制重新登录入口，避免两个调试客户端误用同一 token
- [x] 客户端基础错误响应
  - [x] 房间不存在/已销毁时退回大厅
  - [x] 操作失败时在牌桌上展示明确错误文案
  - [x] 登录过期时退回登录

## P1 联机体验核心

- [x] 使用 WebSocket 通信
  - [x] 客户端通过 `colyseus.js` 连接 Colyseus `mahjong_room`
  - [x] 通过 `mahjong_action` 提交动作
  - [x] 通过 `mahjong_snapshot` 接收各玩家视角快照
  - [x] 客户端已移除 HTTP 轮询兜底
- [x] 在当前操作的玩家头像处增加全员可见的超时倒计时
  - [x] 服务端下发 `turnDeadlineAt`
  - [x] 客户端在当前操作玩家名称旁展示剩余秒数
  - [x] 超时后服务端自动过牌或自动打牌
- [x] 支持玩家新建设置房间配置
  - [x] 房间密码（可选）
  - [x] 超时时间（默认 30 秒）

## P1 架构迁移

- [x] 服务端新增 Colyseus `mahjong_room` 房间类型
  - [x] 使用 `onAuth` 校验 token
  - [x] 使用 `onJoin`/`onLeave` 管理座位恢复、替换登录和退出
  - [x] 通过 `mahjong_action`/`mahjong_snapshot` 通信
- [x] 客户端接入 `colyseus.js`
  - [x] `apps/wx-mahjong` 已声明 `colyseus.js`
  - [x] 客户端用 Colyseus SDK 替换自定义 `/mahjong/ws`
  - [x] 客户端删除 HTTP 轮询兜底
- [x] 服务端删除迁移期兼容层
  - [x] 删除 `MahjongLobby` HTTP 房间状态
  - [x] 删除自定义 `/mahjong/ws`
  - [x] 客户端已改走 Colyseus 房间，运行时真实房间应出现在 `/colyseus` monitor

## P2 玩法实现的问题

- [ ] 碰牌之后不应触发可胡牌
- [ ] 听牌时自摸到红中应可胡牌

## P2 结算与表现

- [ ] 任意方胡牌之后需要明牌至全员可见
- [ ] 增加玩家头像展示

## P3 细节调优

- [ ] 缩短 AI 操作耗时

## 暂不实现

- [ ] 房间持久化
